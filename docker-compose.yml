services:
  clearscan:
    build: .
    container_name: clearscan
    restart: unless-stopped
    ports:
      - "8088:8080"
    volumes:
      - ./data:/data
    environment:
      - JOBS_DIR=/data/jobs
      - MAX_UPLOAD_MB=500
